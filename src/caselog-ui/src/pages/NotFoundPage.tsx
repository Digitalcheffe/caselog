import { RouteState } from '../components/RouteState';

export const NotFoundPage = () => (
  <RouteState title="Log not found" description="The requested route does not exist in this checkpoint." />
);
